[pytest]
; "." for src.tle_fetch-style imports; "src" so tests import api/shared by the
; same module paths the Lambda sees at runtime (/var/task has them at root).
pythonpath = . src
testpaths = tests
