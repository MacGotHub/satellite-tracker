# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# OpenTofu Lambda build artifacts (zipped from src/ at plan/apply time)
opentofu/build/

# OpenTofu local plugin/module cache and any stray state
opentofu/.terraform/
*.tfstate
*.tfstate.*

# Skyfield layer build artifact (rebuilt by src/layers/skyfield/build.py)
src/layers/skyfield/dist/

# Saved plan files
*.tfplan

# Deploy-time inputs (Cesium ion token) — never committed
*.auto.tfvars

# Generated at deploy time by OpenTofu (API URL + Cesium ion token) —
# never committed. Not previously listed here since no one had created a
# local copy before; added while testing the frontend against the live API.
frontend/config.js
